/* Landing page pública — cache enxuto. Dados dinâmicos e rotas internas nunca são pré-cacheados. */
const CACHE='agf-landing-v23-sharedui-nf-reverso-v167';
const STATIC=['/','/index.html','/styles.css','/manifest.webmanifest','/shared/ui/agf-ui.css','/shared/ui/agf-ui.js','/assets/correios-logo-2.png','/assets/logo-agf-jose-bonifacio.png','/assets/whatsapp.svg','/assets/icon-app.svg','/assets/pwa/root/icon-192.png'];
self.addEventListener('install',event=>{self.skipWaiting();event.waitUntil(caches.open(CACHE).then(cache=>Promise.all(STATIC.map(url=>cache.add(url).catch(()=>{})))));});
self.addEventListener('activate',event=>event.waitUntil(Promise.all([caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE&&key.startsWith('agf-landing-')).map(key=>caches.delete(key)))),self.clients.claim()])));
function isInternal(path){return ['/agf/','/app/','/intra/','/atende/','/sla/','/caixa/','/balcao/','/superfrete-admin/','/crm/','/reverso/','/reverso-admin/','/reverso-coleta/','/reverso-expedicao/','/danfe-simplificado/'].some(prefix=>path.indexOf(prefix)===0);}
self.addEventListener('fetch',event=>{const req=event.request;if(req.method!=='GET')return;const url=new URL(req.url);if(url.hostname.includes('script.google.com')||url.hostname.includes('googleusercontent.com')||isInternal(url.pathname))return;if(url.pathname.indexOf('/shared/ui/')===0){event.respondWith(fetch(req).then(res=>{if(res&&res.status===200){var _c=res.clone();caches.open(CACHE).then(k=>k.put(req,_c)).catch(function(){});}return res;}).catch(function(){return caches.match(req);}));return;}if(url.pathname.indexOf('/shared/auth/')===0){event.respondWith(fetch(req,{cache:'no-store'}));return;}if(req.mode==='navigate'){event.respondWith(fetch(req).then(res=>{const copy=res.clone();caches.open(CACHE).then(cache=>cache.put(req,copy)).catch(()=>{});return res;}).catch(()=>caches.match(req).then(hit=>hit||caches.match('/'))));return;}if(url.origin===location.origin){event.respondWith(caches.match(req).then(cached=>cached||fetch(req).then(res=>{if(res&&res.status===200){const copy=res.clone();caches.open(CACHE).then(cache=>cache.put(req,copy)).catch(()=>{});}return res;})));}});
