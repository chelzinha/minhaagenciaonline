{
  "name": "agf-shopify-api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply agf-shopify --local",
    "migrate:remote": "wrangler d1 migrations apply agf-shopify --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
